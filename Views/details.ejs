<!DOCTYPE html>
<html>
<head>
  <!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-S76C7B42DJ"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-S76C7B42DJ');
</script>
  <title>Photo Details</title>
</head>
<body>
  <%- include("./Partials/navadmin.ejs") %>
  <div class="container">
    <h1 class="title">Product Details</h1>
    <div class="product">
      <div class="image-container">
        <img src="/uploads/<%= product.images %>" alt="Product Image" class="image">
      </div>
      <div class="details">
        <p class="name">Item Name: <%= product.itemName %></p>
        <p class="description">Description: <%= product.description %></p>
        <p class="price">Price Before: <%= product.price_before %></p>
        <p class="price">Price After: <%= product.price_after %></p>
        <p class="type">Type: <%= product.type %></p>
      </div>
    </div>

    <div class="buttons">
      <button class="delete" data-linkid="<%= product._id %>">Delete this product</button>
      <button class="edit" data-linkid="<%= product._id %>">Edit this product</button>
    </div>
  </div>

  <script>
    const deleteBtnElement = document.querySelector(".delete");
    const editBtnElement = document.querySelector(".edit");
    const productId = deleteBtnElement.getAttribute("data-linkid");

    deleteBtnElement.addEventListener("click", () => {
      fetch(`/details/${productId}`, { method: "DELETE" })
        .then((response) => response.json())
        .then((data) => (window.location.href = data.mylink))
        .catch((err) => {
          console.log(err);
        });
    });

    editBtnElement.addEventListener("click", () => {
      window.location.href = `/edit-product/${productId}`;
    });
  </script>

  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f5f5f5;
      margin: 0;
      padding: 20px;
    }

    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      background-color: #fff;
      box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
    }

    .title {
      text-align: center;
      margin-top: 0;
    }

    .product {
      display: flex;
      align-items: center;
      margin-bottom: 20px;
    }

    .image-container {
      flex: 0 0 300px;
      margin-right: 20px;
    }

    .image {
      max-width: 100%;
      height: auto;
    }

    .details {
      flex-grow: 1;
    }

    .name {
      font-size: 18px;
      font-weight: bold;
      margin-bottom: 10px;
    }

    .description {
      margin-bottom: 10px;
    }

    .price {
      margin-bottom: 5px;
    }

    .type {
      margin-bottom: 20px;
    }

    .buttons {
      text-align: center;
    }

    .delete,
    .edit {
      padding: 10px 20px;
      background-color: #007bff;
      color: #fff;
      border: none;
      border-radius: 3px;
      cursor: pointer;
      transition: background-color 0.3s, color 0.3s;
      margin-right: 10px;
    }

    .delete:hover,
    .edit:hover {
      background-color: #0056b3;
    }
  </style>
</body>
</html>
